package ingeri;

public class TheDoomer extends Inger {

    public TheDoomer(final String tip, final int x, final int y) {
        setTipInger(tip);
        setX(x);
        setY(y);
        setBun(false);
    }
}
