package ingeri;

public class Dracula extends Inger {

    public Dracula(final String tip, final int x, final int y) {
        setTipInger(tip);
        setX(x);
        setY(y);
        setBun(false);
    }
}
