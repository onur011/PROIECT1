package ingeri;

public class GoodBoy extends Inger {

    public GoodBoy(final String tip, final int x, final int y) {
        setTipInger(tip);
        setX(x);
        setY(y);
        setBun(true);
    }
}
