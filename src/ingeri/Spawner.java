package ingeri;

public class Spawner extends Inger {

    public Spawner(final String tip, final int x, final int y) {
        setTipInger(tip);
        setX(x);
        setY(y);
        setBun(true);
    }
}
